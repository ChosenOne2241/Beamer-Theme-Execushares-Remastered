\documentclass[12pt, twoside]{Misc/Template}
% Feel free to feed the options (font/paper/margin sizes etc.) since `Template` here is merely a superclass of `report`.
% When the `twoside` option is enabled, it is **normal** that the outer page margins will be twice as large as the inner margins.

% All packages below are used here only to generate examples, which are **optional**.
\usepackage{tikz}
\usepackage{lipsum}
\usepackage{graphicx}

\input{Misc/AMS}
% Load AMS math/theorem/font packages and the predefined thereom styles based on AMS theorem.
% If it is unnecessary, just comment out the line above.

\setlength{\headheight}{15pt} % To get rid of the headheight-related warning message caused by `fancyhdr` package.

\begin{document}
	% The front matter.
	\pagenumbering{roman} % Pages for the front matter is roman.
	\generatetitlepage
	\include{Front_Matter/Abstract}

	\tableofcontents
	\listoftables
	\listoffigures
	\include{Front_Matter/Acknowledgements}
	\include{Front_Matter/Dedication}

	% The main matter.
	\pagestyle{fancy}
	\cfoot{}
	\renewcommand{\chaptermark}[1]{\markboth{#1}{#1}}
	\fancyhead[LE, RO]{\thepage}
	\fancyhead[LO, RE]{\textit{\chaptername\ \thechapter: \leftmark}}
	% 4 lines above create the fancy style header for two-side documents.
	\pagenumbering{arabic} % Pages for the main/back matter is arabic.
	\include{Main_Matter/1-Examples}
	\include{Main_Matter/2-Text}

	% The back matter.
	\pagestyle{plain}
	\bibliographystyle{abbrv}
	\bibliography{Back_Matter/Bibliography}
	\bookmarksetupnext{level = part} % `part` is -1.
	\begin{appendices}
		\include{Back_Matter/Appendix_A}
		\include{Back_Matter/Appendix_B}
	\end{appendices}
\end{document}